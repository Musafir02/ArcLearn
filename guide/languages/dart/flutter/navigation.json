{
  "title": "Navigation in Flutter",
  "language": "dart",
  "topic": "navigation",
  "order": 64,
  "nextTopic": "routes",
  "prevTopic": "input_fields",
  "content": "### Navigator\n\n`Navigator` manages a stack of routes. `Navigator.push()` adds a route, `Navigator.pop()` removes it.\n\n```dart\n// Navigate to a new screen\nElevatedButton(\n  onPressed: () {\n    Navigator.push(\n      context,\n      MaterialPageRoute(\n        builder: (context) => const SecondScreen(),\n      ),\n    );\n  },\n  child: const Text('Go to Next'),\n)\n\n// Pop back\nElevatedButton(\n  onPressed: () {\n    Navigator.pop(context);\n  },\n  child: const Text('Go Back'),\n)\n```\n\n### Passing Data Between Screens\n\n```dart\n// Sending data\nNavigator.push(\n  context,\n  MaterialPageRoute(\n    builder: (context) => DetailScreen(item: selectedItem),\n  ),\n);\n\n// Receiving data back\nfinal result = await Navigator.push<String>(\n  context,\n  MaterialPageRoute(builder: (context) => const SelectionScreen()),\n);\n\n// Returning data\nNavigator.pop(context, 'Selected value');\n```\n\n### Named Routes (Basic)\n\n```dart\nMaterialApp(\n  initialRoute: '/',\n  routes: {\n    '/': (context) => const HomeScreen(),\n    '/details': (context) => const DetailScreen(),\n    '/settings': (context) => const SettingsScreen(),\n  },\n)\n\n// Navigate using named route\nNavigator.pushNamed(context, '/details');\n```\n\n### Bottom Navigation Bar\n\n```dart\nclass _HomeState extends State<Home> {\n  int _currentIndex = 0;\n  final _screens = [\n    const HomeScreen(),\n    const SearchScreen(),\n    const ProfileScreen(),\n  ];\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: _screens[_currentIndex],\n      bottomNavigationBar: BottomNavigationBar(\n        currentIndex: _currentIndex,\n        onTap: (index) => setState(() => _currentIndex = index),\n        items: const [\n          BottomNavigationBarItem(icon: Icon(Icons.home), label: 'Home'),\n          BottomNavigationBarItem(icon: Icon(Icons.search), label: 'Search'),\n          BottomNavigationBarItem(icon: Icon(Icons.person), label: 'Profile'),\n        ],\n      ),\n    );\n  }\n}\n```\n\n### Replace and RemoveUntil\n\n```dart\n// Replace current route\nNavigator.pushReplacement(\n  context,\n  MaterialPageRoute(builder: (context) => const NewScreen()),\n)\n\n// Pop back to first route\nNavigator.popUntil(context, (route) => route.isFirst)\n\n// Push and remove all previous\nNavigator.pushAndRemoveUntil(\n  context,\n  MaterialPageRoute(builder: (context) => const HomeScreen()),\n  (route) => false,\n)\n```\n\n> **Key Point:** Use `Navigator.push()` for forward navigation and `Navigator.pop()` to go back. Pass data via constructor parameters. Use `pushReplacement` for login/logout flows.",
  "codeExample": "import 'package:flutter/material.dart';\n\nclass HomeScreen extends StatelessWidget {\n  const HomeScreen({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Home')),\n      body: Center(\n        child: Column(\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            ElevatedButton(\n              onPressed: () async {\n                final result = await Navigator.push<String>(\n                  context,\n                  MaterialPageRoute(\n                    builder: (context) => const DetailScreen(item: 'Hello from Home'),\n                  ),\n                );\n                if (result != null) {\n                  ScaffoldMessenger.of(context).showSnackBar(\n                    SnackBar(content: Text('Returned: $result')),\n                  );\n                }\n              },\n              child: const Text('Open Detail'),\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n}\n\nclass DetailScreen extends StatelessWidget {\n  final String item;\n  const DetailScreen({super.key, required this.item});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Detail')),\n      body: Center(\n        child: Column(\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            Text('Received: $item'),\n            const SizedBox(height: 16),\n            ElevatedButton(\n              onPressed: () => Navigator.pop(context, 'Data back!'),\n              child: const Text('Go Back with Data'),\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n}",
  "quiz": [
    {
      "question": "Which method adds a new route to the navigation stack?",
      "options": ["Navigator.add()", "Navigator.push()", "Navigator.next()", "Navigator.forward()"],
      "correctIndex": 1
    },
    {
      "question": "Which method removes the current route and goes back?",
      "options": ["Navigator.pop()", "Navigator.back()", "Navigator.remove()", "Navigator.exit()"],
      "correctIndex": 0
    }
  ]
}
