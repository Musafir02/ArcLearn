{
  "title": "Buttons in Flutter",
  "language": "dart",
  "topic": "buttons",
  "order": 62,
  "nextTopic": "input_fields",
  "prevTopic": "images_icons",
  "content": "### Button Types\n\nFlutter provides several button widgets following Material Design:\n\n| Button | Description |\n|--------|-------------|\n| `ElevatedButton` | Filled button with shadow |\n| `TextButton` | Text-only button |\n| `OutlinedButton` | Outlined border button |\n| `FilledButton` | Filled button (M3) |\n| `FloatingActionButton` | Circular FAB button |\n| `IconButton` | Icon-only button |\n\n### ElevatedButton\n\n```dart\nElevatedButton(\n  onPressed: () {\n    print('Button pressed');\n  },\n  style: ElevatedButton.styleFrom(\n    padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 12),\n    backgroundColor: Colors.blue,\n    foregroundColor: Colors.white,\n    shape: RoundedRectangleBorder(\n      borderRadius: BorderRadius.circular(8),\n    ),\n  ),\n  child: const Text('Click Me'),\n)\n```\n\n### TextButton and OutlinedButton\n\n```dart\nTextButton(\n  onPressed: () {},\n  child: const Text('Text Button'),\n)\n\nOutlinedButton(\n  onPressed: () {},\n  style: OutlinedButton.styleFrom(\n    side: const BorderSide(color: Colors.blue, width: 2),\n  ),\n  child: const Text('Outlined'),\n)\n```\n\n### Button with Icon\n\n```dart\nElevatedButton.icon(\n  icon: const Icon(Icons.send),\n  label: const Text('Send'),\n  onPressed: () {},\n)\n\nIconButton(\n  icon: const Icon(Icons.favorite),\n  iconSize: 32,\n  color: Colors.red,\n  onPressed: () {},\n  tooltip: 'Add to favorites',\n)\n```\n\n### Button States\n\n```dart\n// Disabled button\nElevatedButton(\n  onPressed: null, // Disabled\n  child: const Text('Disabled'),\n)\n\n// Loading state\nclass _LoadingButton extends StatefulWidget {\n  @override\n  State<_LoadingButton> createState() => _LoadingButtonState();\n}\n\nclass _LoadingButtonState extends State<_LoadingButton> {\n  bool _isLoading = false;\n\n  @override\n  Widget build(BuildContext context) {\n    return ElevatedButton(\n      onPressed: _isLoading\n          ? null\n          : () async {\n              setState(() => _isLoading = true);\n              await Future.delayed(const Duration(seconds: 2));\n              setState(() => _isLoading = false);\n            },\n      child: _isLoading\n          ? const SizedBox(\n              width: 20, height: 20,\n              child: CircularProgressIndicator(strokeWidth: 2),\n            )\n          : const Text('Submit'),\n    );\n  }\n}\n```\n\n| Property | Description |\n|----------|-------------|\n| `onPressed` | Callback when pressed (null = disabled) |\n| `onLongPress` | Callback on long press |\n| `style` | ButtonStyle for customization |\n| `child` | Widget inside the button |\n\n> **Key Point:** Set `onPressed` to `null` to disable a button. Use `ElevatedButton.icon()` for buttons with icons. Customize appearance with the `style` parameter.",
  "codeExample": "import 'package:flutter/material.dart';\n\nclass ButtonDemo extends StatelessWidget {\n  const ButtonDemo({super.key});\n\n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      appBar: AppBar(title: const Text('Buttons')),\n      body: Center(\n        child: Column(\n          mainAxisAlignment: MainAxisAlignment.center,\n          children: [\n            ElevatedButton(\n              onPressed: () => print('Elevated'),\n              child: const Text('Elevated Button'),\n            ),\n            const SizedBox(height: 12),\n            const TextButton(\n              onPressed: null,\n              child: Text('Disabled TextButton'),\n            ),\n            const SizedBox(height: 12),\n            OutlinedButton.icon(\n              icon: const Icon(Icons.add),\n              label: const Text('Add Item'),\n              onPressed: () {},\n            ),\n            const SizedBox(height: 12),\n            ElevatedButton(\n              onPressed: () {},\n              style: ElevatedButton.styleFrom(\n                minimumSize: const Size(200, 48),\n                shape: RoundedRectangleBorder(\n                  borderRadius: BorderRadius.circular(24),\n                ),\n              ),\n              child: const Text('Rounded Button'),\n            ),\n          ],\n        ),\n      ),\n      floatingActionButton: FloatingActionButton(\n        onPressed: () {},\n        child: const Icon(Icons.add),\n      ),\n    );\n  }\n}",
  "quiz": [
    {
      "question": "How do you disable a button in Flutter?",
      "options": ["Set enabled: false", "Set onPressed: null", "Set disabled: true", "Call button.disable()"],
      "correctIndex": 1
    },
    {
      "question": "Which button has a filled background and shadow?",
      "options": ["TextButton", "OutlinedButton", "ElevatedButton", "IconButton"],
      "correctIndex": 2
    }
  ]
}
