{
  "title": "Analytics",
  "language": "nextjs",
  "topic": "analytics",
  "content": "### Google Analytics Integration\n\nAdd Google Analytics to your Next.js app:\n\n```jsx\n// pages/_app.js or app/layout.js\nimport Script from 'next/script';\n\nexport default function App({ Component, pageProps }) {\n  return (\n    <>\n      <Script\n        strategy=\"afterInteractive\"\n        src={`https://www.googletagmanager.com/gtag/js?id=${process.env.NEXT_PUBLIC_GA_ID}`}\n      />\n      <Script id=\"google-analytics\" strategy=\"afterInteractive\">\n        {`\n          window.dataLayer = window.dataLayer || [];\n          function gtag(){dataLayer.push(arguments);}\n          gtag('js', new Date());\n          gtag('config', '${process.env.NEXT_PUBLIC_GA_ID}', {\n            page_path: window.location.pathname,\n          });\n        `}\n      </Script>\n      <Component {...pageProps} />\n    </>\n  );\n}\n```\n\n### Custom Events\n\nTrack user interactions:\n\n```jsx\n// lib/analytics.js\nexport const event = ({ action, category, label, value }) => {\n  if (typeof window !== 'undefined' && window.gtag) {\n    window.gtag('event', action, {\n      event_category: category,\n      event_label: label,\n      value: value,\n    });\n  }\n};\n\n// Usage in component\nimport { event } from '../lib/analytics';\n\nexport default function SignupButton() {\n  const handleClick = () => {\n    event({\n      action: 'signup_click',\n      category: 'engagement',\n      label: 'Homepage CTA',\n    });\n  };\n\n  return <button onClick={handleClick}>Sign Up</button>;\n}\n```\n\n### Page View Tracking\n\nTrack route changes automatically:\n\n```jsx\n// pages/_app.js\nimport { useEffect } from 'react';\nimport { useRouter } from 'next/router';\nimport Script from 'next/script';\n\nexport default function App({ Component, pageProps }) {\n  const router = useRouter();\n\n  useEffect(() => {\n    const handleRouteChange = (url) => {\n      if (window.gtag) {\n        window.gtag('config', process.env.NEXT_PUBLIC_GA_ID, {\n          page_path: url,\n        });\n      }\n    };\n\n    router.events.on('routeChangeComplete', handleRouteChange);\n    return () => router.events.off('routeChangeComplete', handleRouteChange);\n  }, [router.events]);\n\n  return (\n    <>\n      <Script ... />\n      <Component {...pageProps} />\n    </>\n  );\n}\n```\n\n### Vercel Analytics\n\nEnable Vercel Analytics with zero configuration:\n\n1. Go to your Vercel project dashboard\n2. Navigate to the Analytics tab\n3. Enable Analytics\n\nThen add the package:\n\n```bash\nnpm install @vercel/analytics\n```\n\n```jsx\n// app/layout.js\nimport { Analytics } from '@vercel/analytics/react';\n\nexport default function RootLayout({ children }) {\n  return (\n    <html lang=\"en\">\n      <body>\n        {children}\n        <Analytics />\n      </body>\n    </html>\n  );\n}\n```\n\n### Speed Insights\n\nVercel Speed Insights measures Core Web Vitals:\n\n```bash\nnpm install @vercel/speed-insights\n```\n\n```jsx\n// app/layout.js\nimport { SpeedInsights } from '@vercel/speed-insights/next';\n\nexport default function RootLayout({ children }) {\n  return (\n    <html lang=\"en\">\n      <body>\n        {children}\n        <SpeedInsights />\n      </body>\n    </html>\n  );\n}\n```\n\n### Privacy Compliance\n\n- Add cookie consent banner before loading analytics\n- Use `consent` mode in Google Analytics v4\n- Configure IP anonymization\n- Provide opt-out mechanism",
  "codeExample": "// lib/analytics.js\nexport const GA_TRACKING_ID = process.env.NEXT_PUBLIC_GA_ID;\n\n// Track page views\nexport const pageview = (url) => {\n  if (typeof window !== 'undefined' && window.gtag) {\n    window.gtag('config', GA_TRACKING_ID, {\n      page_path: url,\n    });\n  }\n};\n\n// Track custom events\nexport const event = ({ action, category, label, value }) => {\n  if (typeof window !== 'undefined' && window.gtag) {\n    window.gtag('event', action, {\n      event_category: category,\n      event_label: label,\n      value,\n    });\n  }\n};",
  "quiz": [
    {
      "question": "Which next/script strategy should be used for analytics?",
      "options": ["afterInteractive", "beforeInteractive", "lazyOnload", "worker"],
      "correctIndex": 0
    },
    {
      "question": "How do you track page views in a Next.js SPA with GA?",
      "options": ["Listen to router.events routeChangeComplete", "Add a timer that tracks every 5 seconds", "Use a meta tag", "Track on every render"],
      "correctIndex": 0
    }
  ]
}
