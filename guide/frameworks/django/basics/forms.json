{
  "title": "Django Forms",
  "language": "django",
  "topic": "forms",
  "content": "### Forms vs ModelForms\n\nDjango provides two base classes for forms:\n\n- **forms.Form**: Generic form not tied to a model\n- **forms.ModelForm**: Auto-generated form from a model\n\n### Defining a Form\n\n```python\nfrom django import forms\n\nclass ContactForm(forms.Form):\n    name = forms.CharField(max_length=100)\n    email = forms.EmailField()\n    subject = forms.CharField(max_length=200)\n    message = forms.CharField(widget=forms.Textarea)\n    cc_self = forms.BooleanField(required=False)\n```\n\n### Defining a ModelForm\n\n```python\nfrom django import forms\nfrom .models import Post\n\nclass PostForm(forms.ModelForm):\n    class Meta:\n        model = Post\n        fields = ['title', 'slug', 'content', 'category', 'is_published']\n        # exclude = ['author']  # alternative: exclude specific fields\n        widgets = {\n            'content': forms.Textarea(attrs={'rows': 10, 'class': 'rich-text'}),\n            'published_date': forms.DateInput(attrs={'type': 'date'}),\n        }\n        labels = {\n            'is_published': 'Publish immediately?',\n        }\n        help_texts = {\n            'slug': 'URL-friendly version of the title.',\n        }\n```\n\n### Form Fields and Widgets\n\n| Field | Widget | Purpose |\n|-------|--------|---------|\n| `CharField` | `TextInput`, `Textarea` | Text input |\n| `EmailField` | `EmailInput` | Email validation |\n| `IntegerField` | `NumberInput` | Number input |\n| `BooleanField` | `CheckboxInput` | Checkbox |\n| `ChoiceField` | `Select`, `RadioSelect` | Dropdown |\n| `DateField` | `DateInput`, `SelectDateWidget` | Date selection |\n| `FileField` | `ClearableFileInput` | File upload |\n| `ImageField` | `ClearableFileInput` | Image upload |\n| `ModelChoiceField` | `Select` | ForeignKey dropdown |\n| `ModelMultipleChoiceField` | `SelectMultiple` | ManyToMany selection |\n\n### Form Rendering in Templates\n\n```html\n<form method=\"post\" enctype=\"multipart/form-data\">\n  {% csrf_token %}\n\n  <!-- Render all fields automatically -->\n  {{ form.as_p }}\n  {{ form.as_ul }}\n  {{ form.as_div }}\n  {{ form.as_table }}\n\n  <!-- Render individual fields -->\n  <div>\n    {{ form.name.label_tag }}\n    {{ form.name }}\n    {{ form.name.errors }}\n  </div>\n\n  <!-- Manual field rendering -->\n  <div>\n    <label for=\"{{ form.email.id_for_label }}\">Email address:</label>\n    <input type=\"{{ form.email.field.widget.input_type }}\"\n           name=\"{{ form.email.html_name }}\"\n           id=\"{{ form.email.auto_id }}\"\n           value=\"{{ form.email.value|default:'' }}\">\n  </div>\n\n  <button type=\"submit\">Submit</button>\n</form>\n```\n\n### Form Processing in Views\n\n```python\nfrom django.shortcuts import render, redirect\nfrom .forms import ContactForm\n\ndef contact_view(request):\n    if request.method == 'POST':\n        form = ContactForm(request.POST, request.FILES)\n        if form.is_valid():\n            # Access cleaned data\n            name = form.cleaned_data['name']\n            email = form.cleaned_data['email']\n            message = form.cleaned_data['message']\n            # ... process the data\n            return redirect('success')\n    else:\n        form = ContactForm()\n\n    return render(request, 'contact.html', {'form': form})\n```\n\n### Form Validation\n\n**Built-in validators:**\n\n```python\nfrom django.core.validators import MinLengthValidator, EmailValidator, RegexValidator\n\nclass RegistrationForm(forms.Form):\n    username = forms.CharField(\n        max_length=150,\n        validators=[MinLengthValidator(3)]\n    )\n    phone = forms.CharField(\n        validators=[RegexValidator(r'^\\+?1?\\d{9,15}$')]\n    )\n```\n\n**Custom field-level validation:**\n\n```python\nclass NewsletterForm(forms.Form):\n    email = forms.EmailField()\n\n    def clean_email(self):\n        email = self.cleaned_data['email']\n        if not email.endswith('@example.com'):\n            raise forms.ValidationError('Only example.com emails are allowed')\n        return email\n```\n\n**Custom form-level validation:**\n\n```python\nclass PasswordResetForm(forms.Form):\n    password1 = forms.CharField(widget=forms.PasswordInput)\n    password2 = forms.CharField(widget=forms.PasswordInput)\n\n    def clean(self):\n        cleaned_data = super().clean()\n        p1 = cleaned_data.get('password1')\n        p2 = cleaned_data.get('password2')\n        if p1 and p2 and p1 != p2:\n            raise forms.ValidationError('Passwords do not match')\n        return cleaned_data\n```\n\n### Formsets\n\nManage multiple forms on one page:\n\n```python\nfrom django.forms import formset_factory\n\nBookFormSet = formset_factory(BookForm, extra=2)\n\n# In view\nformset = BookFormSet(request.POST or None)\nif formset.is_valid():\n    for form in formset:\n        # process each form\n        pass\n```",
  "codeExample": "class ArticleForm(forms.ModelForm):\n    class Meta:\n        model = Article\n        fields = ['title', 'content', 'category', 'tags']\n        widgets = {\n            'content': forms.Textarea(attrs={'rows': 15}),\n            'tags': forms.CheckboxSelectMultiple(),\n        }\n\n    def clean_title(self):\n        title = self.cleaned_data['title']\n        if len(title) < 5:\n            raise forms.ValidationError('Title must be at least 5 characters.')\n        return title",
  "quiz": [
    {
      "question": "What is the difference between forms.Form and forms.ModelForm?",
      "options": ["There is no difference", "ModelForm is automatically generated from a model definition", "Form is for GET requests and ModelForm is for POST", "ModelForm cannot have custom validation"],
      "correctIndex": 1
    },
    {
      "question": "Which attribute holds validated form data in Django?",
      "options": ["form.validated_data", "form.cleaned_data", "form.clean_data", "form.data"],
      "correctIndex": 1
    }
  ]
}
