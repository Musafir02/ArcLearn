{
  "title": "Authentication",
  "language": "django",
  "topic": "authentication",
  "content": "### The Auth App\n\nDjango's `django.contrib.auth` provides a complete authentication system out of the box. Enable it in `INSTALLED_APPS`:\n\n```python\nINSTALLED_APPS = [\n    'django.contrib.auth',\n    'django.contrib.contenttypes',  # required by auth\n    # ...\n]\n```\n\n### The User Model\n\nThe default `User` model has these fields:\n\n- `username`, `password`, `email`\n- `first_name`, `last_name`\n- `is_active`, `is_staff`, `is_superuser`\n- `date_joined`, `last_login`\n\n### Login and Logout Views\n\nDjango provides built-in auth views:\n\n```python\n# urls.py\nfrom django.urls import path, include\n\nurlpatterns = [\n    path('accounts/', include('django.contrib.auth.urls')),\n]\n```\n\nThis gives you:\n- `accounts/login/`\n- `accounts/logout/`\n- `accounts/password_change/`\n- `accounts/password_reset/`\n\n### Login Template\n\nCreate `registration/login.html`:\n\n```html\n{% if form.errors %}\n  <p class=\"error\">Invalid username or password.</p>\n{% endif %}\n\n<form method=\"post\" action=\"{% url 'login' %}\">\n  {% csrf_token %}\n  {{ form.as_p }}\n  <button type=\"submit\">Log In</button>\n  <input type=\"hidden\" name=\"next\" value=\"{{ next }}\">\n</form>\n```\n\n### Protecting Views\n\n**Function-based views:**\n\n```python\nfrom django.contrib.auth.decorators import login_required\nfrom django.shortcuts import render\n\n@login_required(login_url='/accounts/login/')\ndef dashboard(request):\n    return render(request, 'dashboard.html', {'user': request.user})\n```\n\n**Class-based views:**\n\n```python\nfrom django.contrib.auth.mixins import LoginRequiredMixin\nfrom django.views.generic import TemplateView\n\nclass DashboardView(LoginRequiredMixin, TemplateView):\n    template_name = 'dashboard.html'\n    login_url = '/accounts/login/'\n    redirect_field_name = 'next'\n```\n\n### UserCreationForm\n\n```python\nfrom django.contrib.auth.forms import UserCreationForm\nfrom django.shortcuts import render, redirect\n\ndef register(request):\n    if request.method == 'POST':\n        form = UserCreationForm(request.POST)\n        if form.is_valid():\n            user = form.save()\n            return redirect('login')\n    else:\n        form = UserCreationForm()\n    return render(request, 'registration/register.html', {'form': form})\n```\n\n### Password Change and Reset\n\nBuilt-in views handle the full flow:\n\n```python\n# Password change (logged-in user)\nurlpatterns = [\n    path('password_change/', auth_views.PasswordChangeView.as_view(), name='password_change'),\n    path('password_change/done/', auth_views.PasswordChangeDoneView.as_view(), name='password_change_done'),\n]\n\n# Password reset (forgot password)\nurlpatterns = [\n    path('password_reset/', auth_views.PasswordResetView.as_view(), name='password_reset'),\n    path('password_reset/done/', auth_views.PasswordResetDoneView.as_view(), name='password_reset_done'),\n    path('reset/<uidb64>/<token>/', auth_views.PasswordResetConfirmView.as_view(), name='password_reset_confirm'),\n    path('reset/done/', auth_views.PasswordResetCompleteView.as_view(), name='password_reset_complete'),\n]\n```\n\n### Custom User Model\n\nFor production projects, create a custom user model from the start:\n\n```python\nfrom django.contrib.auth.models import AbstractUser\nfrom django.db import models\n\nclass User(AbstractUser):\n    bio = models.TextField(blank=True)\n    birth_date = models.DateField(null=True, blank=True)\n    phone = models.CharField(max_length=15, blank=True)\n\n    def __str__(self):\n        return self.email\n```\n\nIn `settings.py`:\n\n```python\nAUTH_USER_MODEL = 'myapp.User'\n```\n\nCreate and apply migrations after setting this (do this before migrating for a new project).",
  "codeExample": "from django.contrib.auth.decorators import login_required\nfrom django.contrib.auth.mixins import LoginRequiredMixin\nfrom django.views.generic import ListView\n\n# FBV\n@login_required\ndef profile(request):\n    return render(request, 'profile.html')\n\n# CBV\nclass MyPostsView(LoginRequiredMixin, ListView):\n    model = Post\n    template_name = 'my_posts.html'\n\n    def get_queryset(self):\n        return Post.objects.filter(author=self.request.user)",
  "quiz": [
    {
      "question": "Which decorator is used to restrict a view to authenticated users only?",
      "options": ["@authenticated_only", "@login_required", "@user_passes_test", "@permission_required"],
      "correctIndex": 1
    },
    {
      "question": "What setting is used to specify a custom user model?",
      "options": ["CUSTOM_USER_MODEL", "AUTH_USER_MODEL", "USER_MODEL", "CUSTOM_AUTH_MODEL"],
      "correctIndex": 1
    }
  ]
}
